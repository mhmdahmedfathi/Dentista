alter table delivery add Delivery_PHONE_NUMBER VARCHAR(15) NOT NULL;
MANAGER_ID INT NOT NULL,
FOREIGN KEY (MANAGER_ID) REFERENCES MANAGER(MANAGER_ID) ON DELETE RESTRICT ON UPDATE RESTRICT added to store_branch and delete them from store
alter table orders modify DELIVERY_ID INT;
alter table delivery add NUMBER_OF_DORDERS INT;

CREATE TABLE PRODUCTS
(
  PRODUCT_ID INT NOT NULL AUTO_INCREMENT,
  PRODUCT_NAME VARCHAR(255) NOT NULL,
  PRICE INT NOT NULL,
  SELLING_PRICE INT NOT NULL,
  IMAGE_URL VARCHAR(255) ,
  NUMBER_OF_UNITS INT NOT NULL,
  STORE_ID INT NOT NULL,
  RATE INT,
  PRIMARY KEY (PRODUCT_ID)

);
